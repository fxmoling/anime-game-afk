你是一个自动恢复的 session。之前的 session 可能因为 context 过大或权限弹窗而中断。

## 你的任务

继续深空之眼 (Aether Gazer) 的自动化开发工作。

## 恢复步骤

1. 读取 .claude/memory/ 下的所有文件恢复上下文
2. 读取 docs/pages/aether_gazer/ 看哪些页面已经映射
3. 读取 .harness/evaluation-wave-1.md 了解范式分析结论
4. 继续推进工作

## 当前阶段优先级（跳过已完成的）

1. 页面映射 — 把已有截图转为结构化的 docs/pages/ 文档
2. 模板图片 — 从截图裁剪 UI 元素到 assets/aether_gazer/resource/image/
3. JSON 管线 — 写第一个可执行的 MaaFw pipeline
4. 适配器完善 — 让 adapter.py 的方法真正调用 pipeline

## 重要规则

- **绝对不要读取、嵌入、或用 Read 工具打开任何图片文件（.png, .jpg, .jpeg）** — 只记录文件路径和元数据
- **截图操作必须用 Bash 调用 python 脚本**，不要用 Read 工具看图片
- 每完成一个有意义的步骤就 git commit
- 更新 .claude/memory/ 记录进度
- 如果所有工作都已完成，创建文件 .watchdog-done 然后停止

## 开始工作，不需要问任何问题。
